# Installer copies this to NetworkManager.service.d/ranch-upload-gate.conf.
# Require closed upload rules before NetworkManager can autoactivate profiles.
[Unit]
Requires=ranch-upload-lock.service
After=ranch-upload-lock.service
